name: seek_challenge
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  # Arquitectura y gestión de estado
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  get_it: ^7.6.0
  dartz: ^0.10.1

  # Integración nativa y comunicación entre plataformas
  pigeon: ^22.7.0
  flutter_secure_storage: ^8.0.0

  # Autenticación biométrica
  local_auth: ^2.1.6

  # Almacenamiento local
  sqflite: ^2.3.0
  path: ^1.8.3

  # UI
  cupertino_icons: ^1.0.5
  google_fonts: ^5.1.0
  flutter_svg: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  bloc_test: ^9.1.3
  mockito: ^5.4.2
  build_runner: ^2.4.6

flutter:

  uses-material-design: true
  assets:
    - assets/images/

